# For Java Edition users

## 1.21.7/1.21.8

Try version 1.22.0, as these files are stored in the `1.22.0` directory.

## 1.21.6

Try version 1.21.0, as these files are stored in the `1.21.0` directory.

## 1.21.5

Try version 1.20.0, as these files are stored in the `1.20.0` directory.

## 1.21.4

Try version 1.19.0, as these files are stored in the `1.19.0` directory.

## 1.21.2/1.21.3

Try version 1.18.0, as these files are stored in the `1.18.0` directory.

## 1.21/1.21.1

Try version 1.17.0, as these files are stored in the `1.17.0` directory.

## 1.20.5/1.20.6

Try version 1.16.0, as these files are stored in the `1.16.0` directory.

## 1.20.3/1.20.4

Try version 1.15.0, as these files are stored in the `1.15.0` directory.

## 1.20.2

Try version 1.14.0, as these files are stored in the `1.14.0` directory.

## 1.20/1.20.1

Try version 1.13.0, as these files are stored in the `1.13.0` directory.

## 1.19.4

Try version 1.12.0, as these files are stored in the `1.12.0` directory.

## 1.19.3

Try version 1.11.0, as these files are stored in the `1.11.0` directory.

## 1.19/1.19.1/1.19.2

Try version 1.10.0, as these files are stored in the `1.10.0` directory.

## 1.18/1.18.1/1.18.2

Try version 1.9.0, as these files are stored in the `1.9.0` directory.

## 1.17/1.17.1

Try version 1.8.0, as these files are stored in the `1.8.0` directory.

## 1.16.2/1.16.3/1.16.4/1.16.5

Try version 1.7.0, as these files are stored in the `1.7.0` directory.

## 1.15.x and 1.16/1.16.1

Try version 1.6.0, as these files are stored in the `1.6.0` directory.

## 1.13.x and 1.14.x

Try version 1.5.0, as these files are stored in the `1.5.0` directory.

## 1.12.2 and below

Try version 1.3.0, as these files are stored in the `1.3.0` directory.
